packages:
  - "packages/*"

# Whitelist native build scripts. pnpm 11 introduced this new
# camelCase config (replacing `onlyBuiltDependencies`). The action
# also sets the version, so do NOT add `packageManager: pnpm@X.Y.Z`
# to package.json (it conflicts and CI fails).
allowBuilds:
  "*": true
